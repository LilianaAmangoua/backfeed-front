import {FC} from 'react';
import LoginForm from "../features/auth/LoginForm.tsx";

const LoginPage: FC<{}> = ({}) => {

    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default LoginPage;
